<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Categories</title>
</head>
<body>
    <form>
        <h2>Add Category</h2>
        <input type="text" name="categoryName" id="categoryName" placeholder="Category Name..." required>
        <input type="submit" value="Add Category" id="addCategoryBtn">
    </form>
    <h2>Categories List</h2>
    <table style="border: 1px solid black;">
        <tr>
            <%for( let i = 0;i < Object.keys(result[0]).length; i++){%>
                <th><%= Object.keys(result[0])[i] %></th>
            <%}%>
        </tr>
        <% for(let i = 0; i < result.length; i++){%>
            <tr>
                <form action="/users/<%= Object.values(result[i])[0] %>" method="post"></form>
                <%for(let k = 0; k < Object.values(result[i]).length; k++){%>
                    <td><input type="text" value="<%=Object.values(result[i])[k]%>" ></td>
                <%}%>
                <td>
                    <button>UPDATE</button>
                </form>
                </td>
                <td>
                <form action="/users/<%= Object.values(result[i])[0] %>" >
                    <button>DELETE</button>
                </form>
                </td>
            </tr>
        <%}%>
        <tr>
            <td>
                <form action="">
                    <% if(page > 0) { %>
                        <button name="page" value="<%= Number(page)-1 %>">BACK</button>
                    <% } %>
                        PAGE <%= Number(page)+1 %> IN <%= total_pages %>
                    <% if(page < total_pages - 1) { %>
                        <button name="page" value="<%= Number(page)+1 %>">NEXT</button>
                    <% } %>
                </form>
            </td>
        </tr>
    </table>
</body>
</html>