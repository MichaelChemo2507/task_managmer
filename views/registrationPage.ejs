<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>SIGN UP</title>
    <style>
        *{
            margin: 0px;
            padding: 0px;
            box-sizing: border-box;
            font-family: Arial, sans-serif;
        }
        body{
            display: flex;
            flex-direction: column;
            justify-content:center;
            align-items: center;
            
            background-color: cornflowerblue;
            height: 700px;
        }
        form{
            border-radius: 20px;
            width: 400px;
            height: 550px;
            display: grid;
            align-items: center;
            background-color: white;
        }
        .inputs{
             font-size: large;
            text-align: center;
            margin: 0px auto;
            align-self: end;
            display: block;
            width: 92%;
            max-width: 350px;
            padding: 6px 10px;
            margin-bottom: 14px;
            border: 1px solid #ccc;
            border-radius: 4px;
            transition: border-color 0.2s, box-shadow 0.2s;
        }
        button {

            padding: 8px 16px;
            background-color: #007bff;
            border: none;
            border-radius: 4px;
            color: white;
            cursor: pointer;
            transition: background-color 0.3s;
            margin: 0px auto;
        }
          button:hover {
              background-color: #0056b3;
            }
        h2{
            margin-bottom: 10px;
        }
        
        a{
            padding-left: 40px;
        }
        a:link {color:#ff0000;text-decoration:none;}
        a:visited {color:cornflowerblue;text-decoration:none;}
        a:hover {text-decoration:underline;}
    </style>
</head>
<body>
<h2>RAGISTRATION</h2>
<form method="<%= data.method %>" action="<%= data.URL %>">
    <% if (data.errorMessage) { %>
        <h4 style="color: rgb(254, 60, 60);text-align: center;"><%= data.errorMessage %></h4>
    <% } %>
    <input class="inputs" type="text" name="userName" id="UserName" placeholder="user name...">
    <input class="inputs" type="email" name="email" id="email" placeholder="email...">
    <input class="inputs" type="password" name="password" id="password" placeholder="password...">
    <button><%= data.btnText %></button>
    <a href="<%= data.LOGIN_URL %>">Back To Login</a>
</form>
</form>
</body>
</html>